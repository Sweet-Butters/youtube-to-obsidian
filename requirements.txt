auto_project @ git+https://github.com/Sweet-Butters/auto_project.git@v0.5.0
youtube-transcript-api>=1.2.0
yt-dlp>=2024.10.0
requests>=2.32
PyYAML>=6

# Optional: local Whisper transcription for caption-less videos.
# Only installed/used when ENABLE_WHISPER=1 (set on the self-hosted workflow).
# Comment out on machines where you don't want the ~150MB model download.
faster-whisper>=1.0.0; sys_platform != "win32" or python_version >= "3.11"
